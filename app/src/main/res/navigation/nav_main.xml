<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/saints_nav_graph"
    app:startDestination="@+id/fragment_login">

    <fragment
        android:id="@+id/fragment_login"
        android:name="com.example.bycoders.presentation.ui.implementation.AuthenticationFragmentImpl"
        tools:layout="@layout/auth_fragment">

        <action
            android:id="@+id/to_map"
            app:destination="@id/fragment_map" />

    </fragment>

    <fragment
        android:id="@+id/fragment_map"
        android:name="com.example.bycoders.presentation.ui.implementation.HomeFragmentImpl"
        tools:layout="@layout/home_fragment" />

</navigation>
